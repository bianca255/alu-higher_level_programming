#!/bin/bash
# Script to execute the Python file defined by the $PYFILE environment variable

if [ -z "$PYFILE" ]; then
  echo "PYFILE environment variable is not set."
  exit 1
fi

# Run the Python file
python3 "$PYFILE"

